import React from 'react'

const DemoHeader = () => (
  <div style={{
    display: 'flex',
    justifyContent: 'center',
    backgroundColor: 'yellow',
  }}
  >
    This shopping platform is a proof of concept. Sign up using fake email.
  </div>
)

export default DemoHeader
